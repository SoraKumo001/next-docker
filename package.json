{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "docker:start": "docker compose -p next-test -f docker/docker-compose.yml up ",
    "docker:stop": "docker compose -p next-test -f docker/docker-compose.yml down",
    "docker:build": "docker compose -p next-test -f docker/docker-compose.yml build --parallel --compress --pull"
  },
  "devDependencies": {
    "@types/node": "20.3.1",
    "@types/react": "18.2.14",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "next": "^13.4.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
